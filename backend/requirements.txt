fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0
supabase>=2.4
httpx>=0.27
numpy>=1.23
pandas>=1.5
# torch is needed for the embedding encoder; install the CPU wheel if you don't have it:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0
